general {
    output_format = "dzen2"
    colors = true
    interval = 5
}

order += "cpu_usage"
order += "cpu_temperature 0"
order += "time"

cpu_usage {
  format = "CPU %usage"
}

cpu_temperature 0{
  format = "T: %degrees"
}

time {
  format = "%a %Y-%m-%d %I:%M"
}
