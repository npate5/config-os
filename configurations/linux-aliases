#install 
#alias installAll=`sudo apt-get install -y --assume-yes unzip htop curl wget build-essential git vim zsh nodejs npm calibre gnome-tweak-tool tree`
#alias i3InstallAll='sudo apt-get install -y --assume-yes xorg lightdm i3 rofi gnome-terminal firefox thunar compton feh lxappearance xinit gnome-control'
alias setupZsh='wget https://github.com/robbyrussell/oh-my-zsh/raw/master/tools/install.sh -O - | zsh && chsh -s $(which zsh)'

#ubuntu/debian
alias aptget='sudo apt-get -y install'
alias aptgetforce='sudo apt-get install -f'
alias upgrade='sudo apt-get upgrade'
alias update='sudo apt-get update'
alias off='sudo shutdown -h now'
alias shut='systemctl suspend'
alias reboot='sudo shutdown -r now'
alias offCancel='sudo shutdown -c'
alias openthis="nautilus ."
alias ci3="code ~/.config/i3/config"
alias ccompton="code ~/.config/compton.conf"
alias gi3="gedit ~/.config/i3/config"
alias gcompton="gedit ~/.config/compton.conf"
alias xwin="xwininfo " 

#java
#export JAVA_HOME=/usr/local/jdk-1.8.0
#export PATH=$JAVA_HOME/bin:$PATH:


extract() { 
    #Use: extract archived_file_name.extension
    if [ -f $1 ] ; then 
      case $1 in 
        *.tar.bz2)   tar xjf $1     ;; 
        *.tar.gz)    tar xzf $1     ;; 
        *.bz2)       bunzip2 $1     ;; 
        *.rar)       unrar e $1     ;; 
        *.gz)        gunzip $1      ;; 
        *.tar)       tar xf $1      ;; 
        *.tbz2)      tar xjf $1     ;; 
        *.tgz)       tar xzf $1     ;; 
        *.zip)       unzip $1       ;; 
        *.Z)         uncompress $1  ;; 
        *.7z)        7z x $1        ;; 
        *)     echo "'$1' cannot be extracted via extract()" ;; 
         esac 
     else 
         echo "'$1' is not a valid file" 
     fi 
}

#various
alias cpuFrequencyMonitor='watch -n1 "cat /proc/cpuinfo | grep \"^[c]pu MHz\""'

#xbindkeys
